{
  "name": "placement-week-scheduler-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend && cp -r frontend/dist dist"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.4"
  }
}
